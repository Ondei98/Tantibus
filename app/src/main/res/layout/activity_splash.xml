<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SplashActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/splash_image_button"
            android:layout_width="@dimen/splash_icon"
            android:layout_height="@dimen/splash_icon"
            android:layout_centerInParent="true"
            android:background="@mipmap/ic_launcher_round" />

        <TextView
            android:id="@+id/credits_name"
            style="@style/Splash_textView_style"
            android:layout_below="@+id/splash_image_button"
            android:layout_marginTop="30dp"
            android:text="@string/app_name" />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

